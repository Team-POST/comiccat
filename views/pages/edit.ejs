<!-- /edit/:id : Editable of Selected Comic (GET) TODO: -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Your ComicCat</title>
    <link href="styles/reset.css" rel="stylesheet"/> <!-- ? (applies to all links) does it need: styles/  -->
    <link href="styles/base.css" rel="stylesheet"/>
    <link rel="stylesheet" href="styles/layout.css"/>
    <link rel="stylesheet" href="styles/modules.css"/>
</head>
<body>

    <%- include('../layout/header.ejs') %>

    <h1>HOWDY!</h1>
    <form id="editbox" method="PUT" action="/edit/:id">
        <h3 id="title"><a href="<%= incomingComic.comic_url%>"><% incomingComic.title %></a></h3> <!-- Now the title *should be* the link to the Marvel.com page for the comic book/hero (if we reach that stretch goal). -->
        <fieldset>
            <textarea id="description" name="description" value="<%= incomingComic.description %>" rows="5" cols="25"><%= incomingComic.description %></textarea>
            <!-- TODO: AND the notes -->
        </fieldset>

        <button>Update</button>
    </form>
</body>
</html>