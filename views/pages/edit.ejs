<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../layout/head.ejs') %>
    <title>Edit Your ComicCat</title>
</head>
<body>
    <%- include('../layout/header.ejs') %>
    <section id="editSection">
        <img id="title" src="<%= incomingComic.comic_url%>" alt="<%= incomingComic.comic_url%>">
        <form id="editbox" action="/edit/<%= incomingComic.id %>?_method=PUT" method="post"> 
            <fieldset>
                <label for="description">Description</label>

                <textarea id="description" name="description" value="<%= incomingComic.description %>" rows="20" cols="30"><%= incomingComic.description %></textarea>

                <label for="notes">Notes</label>

                <textarea id="notes" name="notes" value="<%= incomingComic.notes %>" rows="20" cols="35" placeholder="Write your notes here"></textarea>
            </fieldset>
            <button type="submit">Update</button>
        </form>
    </section>
</body>
</html>